apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.Source | dump }}.${{ values.Schema | dump }}.${{ values.Table | dump }}
  description: |
    ${{ values.Description }}
  annotations:
    backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: user:guest
  lifecycle: POC
